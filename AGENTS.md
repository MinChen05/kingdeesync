## 项目说明
这是一个金蝶数据同步工具，主要包含 API 拉取、SQL Server 写入、桌面 GUI。

## 沟通要求
- 默认用中文回复
- 先给结论，再给细节
- 每次会话末尾都要提供“下一步提示词”，方便用户直接复制继续；如果确实没有下一步，则明确说明“暂无下一步提示词”，并说明原因
- 变更前如果涉及数据库结构，先说明影响

## 工作方式
- 先定位根因，再修改
- 优先做最小改动
- 不做无关重构
- 改性能问题时，先加日志或证据

## 数据库规则
- 允许新增索引
- 允许调整非报表表结构
- 报表相关表不要改结构
- 涉及主键、唯一键、字段顺序调整时，要保留数据并校验行数

## 验证要求
改完后至少执行：
- 相关单元测试
- 相关脚本 dry-run
- 如涉及 SQL Server 写入，说明预期日志变化

## 禁止事项
- 不要执行破坏性删除
- 不要清空生产数据
- 不要提交密钥或密码
